@model List<Patient>
@{
    ViewData["Title"] = "Hasta Listesi";
}
<div class="page-head"><div><h1>Hasta Listesi</h1><div class="crumb">Muayene ettiğiniz hastalar</div></div></div>

<div class="card-glass">
    @if (Model.Count == 0)
    {
        <div class="empty-state"><i class="fa-solid fa-hospital-user"></i><div>Henüz hasta kaydınız yok.</div></div>
    }
    else
    {
        <div class="table-wrap">
            <table class="data">
                <thead><tr><th>Hasta</th><th>TC Kimlik</th><th>Telefon</th><th>Kan Grubu</th><th></th></tr></thead>
                <tbody>
                @foreach (var p in Model)
                {
                    var initials = string.Join("", p.FullName.Split(' ', StringSplitOptions.RemoveEmptyEntries).Take(2).Select(x => x[0]));
                    <tr>
                        <td><div class="list-avatar"><div class="avatar">@initials.ToUpper()</div><div class="meta"><div class="n">@p.FullName</div><div class="s">@p.Email</div></div></div></td>
                        <td>@p.TcKimlikNo</td>
                        <td>@(string.IsNullOrEmpty(p.Phone) ? "-" : p.Phone)</td>
                        <td>@(string.IsNullOrEmpty(p.BloodType) ? "-" : p.BloodType)</td>
                        <td><a asp-action="PatientHistory" asp-route-id="@p.Id" class="btn btn-sm btn-soft"><i class="fa-solid fa-clock-rotate-left"></i> Geçmiş</a></td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
</div>
